import ContactClient from "./ContactClient"

export const metadata = {
  title: "Contact | Get in Touch with TSS GNDU",
  description: "Contact Technical Students' Society GNDU for collaborations, queries, or event participation. Reach out to our team at Guru Nanak Dev University, Amritsar."
}

export default async function AboutPage() {
  return <ContactClient />;
}
